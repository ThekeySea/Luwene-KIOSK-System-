{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "apt-get update -qq && apt-get install -y -qq php-cli php-mbstring php-xml php-curl php-zip php-sqlite3 unzip && curl -sS https://getcomposer.org/installer | php -- --install-dir=/usr/local/bin --filename=composer && composer install --no-dev --prefer-dist --optimize-autoloader",
  "buildCommand": "npm ci && npm run build",
  "outputDirectory": "public",
  "functions": {
    "api/index.php": {
      "runtime": "vercel-php@0.7.4"
    }
  },
  "rewrites": [
    { "source": "/storage/(.*)", "destination": "/storage/$1" },
    { "source": "/((?!api/).*)", "destination": "/api/index.php" }
  ]
}
